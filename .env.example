# Copy to .env and fill in. The pipeline reads these via pydantic-settings.
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
# Optional: point at a running Qdrant server to persist the index.
# Leave unset to use in-memory mode (zero external dependencies).
FINRAG_QDRANT_URL=
